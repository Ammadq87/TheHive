export default function Login (){
    return (
        <div>
            <form action="">
                <input type="email" required/>
                <button type="submit">Sign In</button>
            </form>
        </div>
    );
}