import SideBar from "../../components/SideBar";

export default function Spaces() {
    return (
        <div id='Spaces' className="flex overflow-hidden">
            <SideBar/>
            <h1>SpaceName</h1>

            <h2>Overview</h2>
        </div>
    );
}